import 'dart:ui';
import 'package:flutter/cupertino.dart';

class ViewConst {
  //屏幕的物理值（1x）
  static Size screenSize = Size(window.physicalSize.height/window.devicePixelRatio, window.physicalSize.width/window.devicePixelRatio);

}